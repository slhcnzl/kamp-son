 <%= simple_form_for(@notice) do |f| %>
    <%= f.input :title, label: "Başlık:", placeholder: "Lütfen başlık giriniz!" %>
    <%= f.input :message, label: "İlan İçeriği" %>
    <%= f.button :submit, "Gönder", :class => "btn btn-primary" %>
<% end %>